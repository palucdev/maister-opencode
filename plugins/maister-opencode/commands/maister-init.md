---
name: maister-init
description: Initialize AI SDLC framework with intelligent project analysis and documentation generation
---

You MUST invoke the `init` skill as your FIRST and only action. Use the `skill` tool immediately:

```
skill({ name: "init" })
```

Do not analyze the project first. Do not ask questions first. Invoke the skill directly — it handles all initialization steps interactively.
